const canvas=document.getElementById('editorCanvas');const view=new View3D(canvas);const scene={};const editor=new Editor(scene,null,canvas);const sceneController=new SceneController(scene,editor);const io=new IOController(editor,sceneController);const ui=new UIController(editor,sceneController,io);const selection=new SelectionController(editor,view);editor.sceneController=sceneController;editor.ui=ui;const input=new InputController(editor,canvas,view,selection);const fbx=new FBXImporter(editor,sceneController,ui);const fbxBtn=document.getElementById('importFbxBtn'),fbxInput=document.getElementById('fbxInput');if(fbxBtn&&fbxInput){fbxBtn.addEventListener('click',()=>fbxInput.click());fbxInput.addEventListener('change',async()=>{const file=fbxInput.files&&fbxInput.files[0];if(!file)return;try{await fbx.importFile(file)}catch(err){alert('FBX import failed: '+(err&&err.message?err.message:err))}fbxInput.value=''})}document.querySelectorAll('.mode-choice').forEach(btn=>btn.addEventListener('click',()=>input.setMode(btn.dataset.mode)));document.addEventListener('modechanged',e=>{document.querySelectorAll('.mode-choice').forEach(b=>b.classList.toggle('active',b.dataset.mode===e.detail.mode))});
// Always begin a fresh session with exactly one default cube.
sceneController.objects=[];editor.selectObject(null);sceneController.addCube('Cube');ui.refresh();
function resize(){view.resize()}window.addEventListener('resize',resize);function frame(){view.render(sceneController.objects,editor);requestAnimationFrame(frame)}frame();