import{allBlocks}from'./blocks.js';
export class Graph{constructor(el){this.el=el;this.nodes=[];this.selected=null}add(name,x=40,y=40,params={}){const n={id:crypto.randomUUID(),name,x,y,params:{...params}};this.nodes.push(n);this.selected=n;this.draw();return n}clear(){this.nodes=[];this.selected=null;this.draw()}draw(){this.el.innerHTML='';if(!this.nodes.length){this.el.innerHTML='<div class="empty-graph"><b>No blocks in the graph</b><span>Click a block on the left to add it.</span></div>';return}for(const n of this.nodes){const d=document.createElement('div');d.className='node'+(this.selected?.id===n.id?' selected':'');d.style.left=Math.max(8,n.x)+'px';d.style.top=Math.max(8,n.y)+'px';d.dataset.id=n.id;d.innerHTML=`<div class="node-head"><span class="node-dot"></span><b>${n.name}</b><button class="node-delete" title="Delete">×</button></div><div class="node-body"><div><span class="socket input"></span><span>Input</span></div><div class="node-value">${this.valueEditor(n)}</div><div><span class="socket output"></span><span>Output</span></div></div>`;d.onclick=e=>{if(!e.target.closest('button')){this.selected=n;this.draw()}};d.querySelector('.node-delete').onclick=e=>{e.stopPropagation();this.nodes=this.nodes.filter(x=>x.id!==n.id);this.selected=null;this.draw()};const editor=d.querySelector('[data-param]');if(editor)editor.oninput=()=>{n.params.value=Number(editor.value)||0};d.onpointerdown=e=>{if(e.target.closest('button,input'))return;e.preventDefault();this.selected=n;const sx=e.clientX,sy=e.clientY,ox=n.x,oy=n.y;const move=q=>{n.x=ox+q.clientX-sx;n.y=oy+q.clientY-sy;d.style.left=Math.max(8,n.x)+'px';d.style.top=Math.max(8,n.y)+'px'};const up=()=>{document.removeEventListener('pointermove',move);document.removeEventListener('pointerup',up);this.draw()};document.addEventListener('pointermove',move);document.addEventListener('pointerup',up)};this.el.appendChild(d)}}valueEditor(n){const names=['Move','Scale','Scale X','Scale Y','Scale Z','Rotate X','Rotate Y','Rotate Z','Bevel','Extrude','Inset'];if(names.includes(n.name))return`<label>Value <input data-param type="number" value="${n.params.value??1}" step="0.1"></label>`;return'<span class="muted">ready</span>'}}
export function populatePalette(root,graph){root.innerHTML='';for(const[cat,names]of Object.entries(grouped())){const section=document.createElement('section');section.className='palette-section';const title=document.createElement('button');title.className='category-title';title.innerHTML=`<span>${cat}</span><span>⌄</span>`;const list=document.createElement('div');list.className='palette-list';for(const name of names){const b=document.createElement('button');b.className='block-button';b.dataset.block=name;b.innerHTML=`<span class="block-icon">${icon(cat)}</span><span>${name}</span>`;b.onclick=()=>graph.add(name,80+Math.random()*180,50+Math.random()*140);list.appendChild(b)}title.onclick=()=>section.classList.toggle('collapsed');section.append(title,list);root.appendChild(section)}function grouped(){const out={};for(const b of allBlocks)(out[b.category]??=[]).push(b.name);return out}function icon(cat){return({Primitives:'◆',Transforms:'↗',Mesh:'◇',Faces:'▣',Edges:'━',Vertices:'•',Math:'ƒ',Logic:'?',Scene:'▦',Material:'●',Data:'D',Modifiers:'M',Output:'→'})[cat]||'•'}}