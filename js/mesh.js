export const V={add:(a,b)=>a+b,sub:(a,b)=>a-b,mul:(a,b)=>a*b,div:(a,b)=>b?a/b:0,pow:Math.pow,min:Math.min,max:Math.max,abs:Math.abs,sin:x=>Math.sin(x*Math.PI/180),cos:x=>Math.cos(x*Math.PI/180),sqrt:Math.sqrt,clamp:(x,a,b)=>Math.max(a,Math.min(b,x))};
export function v3(x=0,y=0,z=0){return{x,y,z}}export function cloneMesh(m){return{vertices:m.vertices.map(v=>({...v})),faces:m.faces.map(f=>[...f])}}
export const Mesh={cube:(sx=1,sy=1,sz=1)=>{sx/=2;sy/=2;sz/=2;let v=[[-sx,-sy,-sz],[sx,-sy,-sz],[sx,sy,-sz],[-sx,sy,-sz],[-sx,-sy,sz],[sx,-sy,sz],[sx,sy,sz],[-sx,sy,sz]].map(([x,y,z])=>v3(x,y,z));return{vertices:v,faces:[[0,1,2,3],[4,7,6,5],[0,4,5,1],[1,5,6,2],[2,6,7,3],[4,0,3,7]]}},plane:(s=2)=>({vertices:[v3(-s/2,0,-s/2),v3(s/2,0,-s/2),v3(s/2,0,s/2),v3(-s/2,0,s/2)],faces:[[0,1,2,3]]}),sphere:(r=1,seg=16,rings=10)=>{let v=[],f=[];for(let y=0;y<=rings;y++){let a=y*Math.PI/rings;for(let x=0;x<seg;x++){let b=x*2*Math.PI/seg;v.push(v3(r*Math.sin(a)*Math.cos(b),r*Math.cos(a),r*Math.sin(a)*Math.sin(b)))}}for(let y=0;y<rings;y++)for(let x=0;x<seg;x++){let n=(x+1)%seg,a=y*seg+x,b=y*seg+n,c=(y+1)*seg+n,d=(y+1)*seg+x;f.push([a,b,c,d])}return{vertices:v,faces:f}},cylinder:(r=1,h=2,seg=16)=>{let v=[],f=[];for(let y of[-h/2,h/2])for(let i=0;i<seg;i++){let a=i*2*Math.PI/seg;v.push(v3(r*Math.cos(a),y,r*Math.sin(a)))}for(let i=0;i<seg;i++){let n=(i+1)%seg;f.push([i,n,seg+n,seg+i])}f.push([...Array(seg).keys()].reverse(),[...Array(seg).keys()].map(i=>seg+i));return{vertices:v,faces:f}}};