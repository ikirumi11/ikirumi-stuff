export function download(name,text,type='application/json'){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}export function meshJSON(ms){return JSON.stringify(ms,null,2)}export function obj(ms){let out=[],faces=[],o=1;for(const m of ms){for(const p of m.vertices)out.push(`v ${p.x} ${p.y} ${p.z}`);for(const f of m.faces)faces.push('f '+f.map(i=>i+o).join(' '));o+=m.vertices.length}return out.concat(faces).join('\n')}